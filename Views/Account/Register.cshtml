@model start.Models.Customer
@{
    ViewData["Title"] = "Register";
}

<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Muli:wght@400;600;700&display=swap">
<link rel="stylesheet" href="/css/Account/login.css" />
<link rel="stylesheet" href="/css/Account/register.css" />
<div class="login-form">
    <div class="background"></div>
    <div class="login-box">
        <div class="top">
            <img src="https://res.cloudinary.com/do48qpmut/image/upload/v1761645429/uploads/tdppvgyas8bhvfs7lton.png" alt="Logo">         
                <div class="brand">
                    <div class="title register-title">
                        <span class="line1">ĐĂNG</span>
                        <span class="line2">KÝ</span>
                    </div>           
            </div>
        </div>

        <form asp-action="Register" asp-controller="Account" method="post">

            <div class="mb-3 input-group">
                <span class="input-group-text"><i class="fa-solid fa-user"></i></span>
                <input asp-for="Name" class="form-control" placeholder="Tên khách hàng" required />
            </div>
            <span class="text-danger small validation-error">@Html.ValidationMessageFor(m => m.Name)</span>

            <div class="mb-3 input-group">
                <span class="input-group-text"><i class="fa-solid fa-phone"></i></span>
                <input asp-for="Phone" class="form-control" placeholder="Số điện thoại" required />
            </div>
            <span class="text-danger small validation-error">@Html.ValidationMessageFor(m => m.Phone)</span>

            <div class="mb-3 input-group">
                <span class="input-group-text"><i class="fa-solid fa-envelope"></i></span>
                <input asp-for="Email" class="form-control" placeholder="Email" required />
            </div>
            <span class="text-danger small validation-error">@Html.ValidationMessageFor(m => m.Email)</span>

            <div class="mb-3 input-group">
                <span class="input-group-text"><i class="fa-solid fa-id-card"></i></span>
                <input asp-for="Username" class="form-control" placeholder="Tên đăng nhập" required />
            </div>
            <span class="text-danger small validation-error">@Html.ValidationMessageFor(m => m.Username)</span>

            <div class="mb-3 input-group">
                <span class="input-group-text"><i class="fa-solid fa-lock"></i></span>
                <input asp-for="Password" type="password" class="form-control" placeholder="Mật khẩu" required minlength="7" />
            </div>
            <span class="text-danger small validation-error">@Html.ValidationMessageFor(m => m.Password)</span>

            <button type="submit" class="btn-login btn-main w-100">Đăng ký</button>
        </form>

        <div class="extra-links">
            <p>Đã có tài khoản? <a asp-controller="Account" asp-action="Login" class="alt">Đăng nhập</a></p>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    
    <script>
    document.addEventListener('DOMContentLoaded', function() {
        const form = document.querySelector('form');
        const inputs = form.querySelectorAll('input[required]');
        const phoneInput = document.querySelector('input[name="Phone"]');
        
        // Add real-time validation
        inputs.forEach(input => {
            input.addEventListener('blur', function() {
                validateField(this);
            });
            
            input.addEventListener('input', function() {
                if (this.classList.contains('is-invalid')) {
                    validateField(this);
                }
            });
        });
        
        // Form submission validation
        form.addEventListener('submit', function(e) {
            let isValid = true;
            
            inputs.forEach(input => {
                if (!validateField(input)) {
                    isValid = false;
                }
            });
            
            // Validate phone specifically
            if (phoneInput) {
                const phone = phoneInput.value.trim();
                if (!phone) {
                    phoneInput.closest('.input-group').classList.add('error');
                    phoneInput.classList.add('is-invalid');
                    isValid = false;
                } else if (phone.length < 10) {
                    const phoneErrorSpan = phoneInput.closest('.input-group').nextElementSibling;
                    if (phoneErrorSpan) {
                        phoneErrorSpan.textContent = 'Số điện thoại phải có ít nhất 10 số.';
                        phoneErrorSpan.style.display = 'block';
                    }
                    phoneInput.closest('.input-group').classList.add('error');
                    phoneInput.classList.add('is-invalid');
                    isValid = false;
                } else if (!/^[0-9]{10,11}$/.test(phone)) {
                    const phoneErrorSpan = phoneInput.closest('.input-group').nextElementSibling;
                    if (phoneErrorSpan) {
                        phoneErrorSpan.textContent = 'Số điện thoại không hợp lệ. Vui lòng nhập 10 hoặc 11 số.';
                        phoneErrorSpan.style.display = 'block';
                    }
                    phoneInput.closest('.input-group').classList.add('error');
                    phoneInput.classList.add('is-invalid');
                    isValid = false;
                }
            }
            
            if (!isValid) {
                e.preventDefault();
            }
        });
        
        function validateField(input) {
            const inputGroup = input.closest('.input-group');
            const value = input.value.trim();
            
            if (input.hasAttribute('required') && !value) {
                inputGroup.classList.add('error');
                input.classList.add('is-invalid');
                return false;
            } else {
                inputGroup.classList.remove('error');
                input.classList.remove('is-invalid');
                return true;
            }
        }
        
        // Email validation
        const emailInput = document.querySelector('input[type="email"], input[name="Email"]');
        if (emailInput) {
            emailInput.addEventListener('blur', function() {
                const email = this.value.trim();
                const hasAt = email.includes('@@');
                const hasDot = email.includes('.');
                const isValidEmail = hasAt && hasDot && email.length > 5;
                
                if (email && !isValidEmail) {
                    this.closest('.input-group').classList.add('error');
                    this.classList.add('is-invalid');
                }
            });
        }
        
        // Phone validation
        if (phoneInput) {
            const phoneErrorSpan = phoneInput.closest('.input-group').nextElementSibling;
            
            function validatePhone() {
                const phone = phoneInput.value.trim();
                const phoneRegex = /^[0-9]{10,11}$/;
                const inputGroup = phoneInput.closest('.input-group');
                
                if (!phone) {
                    inputGroup.classList.remove('error');
                    phoneInput.classList.remove('is-invalid');
                    if (phoneErrorSpan) {
                        phoneErrorSpan.textContent = '';
                        phoneErrorSpan.style.display = 'none';
                    }
                    return true;
                }
                
                if (phone.length < 10) {
                    inputGroup.classList.add('error');
                    phoneInput.classList.add('is-invalid');
                    if (phoneErrorSpan) {
                        phoneErrorSpan.textContent = 'Số điện thoại phải có ít nhất 10 số.';
                        phoneErrorSpan.style.display = 'block';
                    }
                    return false;
                }
                
                if (!phoneRegex.test(phone)) {
                    inputGroup.classList.add('error');
                    phoneInput.classList.add('is-invalid');
                    if (phoneErrorSpan) {
                        phoneErrorSpan.textContent = 'Số điện thoại không hợp lệ. Vui lòng nhập 10 hoặc 11 số.';
                        phoneErrorSpan.style.display = 'block';
                    }
                    return false;
                }
                
                inputGroup.classList.remove('error');
                phoneInput.classList.remove('is-invalid');
                if (phoneErrorSpan) {
                    phoneErrorSpan.textContent = '';
                    phoneErrorSpan.style.display = 'none';
                }
                return true;
            }
            
            phoneInput.addEventListener('blur', validatePhone);
            phoneInput.addEventListener('input', function() {
                if (this.value.trim().length > 0) {
                    validatePhone();
                }
            });
        }
    });
    </script>
}